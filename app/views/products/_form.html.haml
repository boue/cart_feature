/ multipart: true is necessary since we are uploading a file in our form 
= form_for @product, multipart: true do |f|
  - if @product.errors.any?
    #error_explanation
      %h2= "#{pluralize(@product.errors.count, "error")} prohibited this product from being saved:"
      %ul
        - @product.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :price
    = f.text_field :price
  .field
    = f.label :image
    / helper method to create the upload file icon :image is the resource 
    = f.file_field :image
    %br/
  .actions
    = f.submit 'Save'
